import React from 'react';
import { Col } from 'react-bootstrap';

const Manage = props => (
  <React.Fragment>
    <div>
      <Col>
        <h2>Manage</h2>
      </Col>
    </div>
  </React.Fragment>
);

export default Manage;
